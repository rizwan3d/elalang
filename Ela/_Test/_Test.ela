/*open Core

let extends
    (Foo x) * y = x * y
 et (Foo x) + y = x + y
    
let xs = [Foo 1, Foo 2, Foo 3]//(Foo 2) :: [1..10]
take 10 <| map (+2) xs
*/
open Core

let x = 12 `even`
        where even x = x+x
        
x