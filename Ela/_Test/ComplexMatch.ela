type Maybe = Yes(x) | No(x) | Maybe(x);

let mb = Maybe.Maybe(1233);//Maybe.Yes(2);

var r = match(mb) 
	| Yes(x) with op = "yes" | Maybe(x) with op = "maybe" -> "Yes/Maybe:" + op + ";Val=" + string(x)
	| _ -> "none";
	
cout r;
	