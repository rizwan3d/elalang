var Token = template
{
	Uid = 0
};

var User = template : Token
{
	FirstName = "Basil",
	LastName = "Voronkov",
	IsInRole = function(role)
	{
		return role == "Admin";
	}	
};

var usrTemplate = User;

var obj = new : usrTemplate
{
	GetFullName = function()
	{
		return this.LastName + ", " + this.FirstName;
	}
};

var f = fun(x, y) x + y;

print( obj.FirstName );
read();
return obj.FirstName = "Vasily";