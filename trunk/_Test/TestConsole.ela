TestInput(20);

function TestInput(iter)
{
	for (var i = 0; i < iter; i++)
	{
		print("Test input #" + i + ":");
		var in = read();
		print("You've entered: " + in);
	}
}


function TestInput2(iter)
{
	for (var i = 0; i < iter; i++)
	{
		print("Test input #" + i + ":");
		print("You've entered: " );
	}
}