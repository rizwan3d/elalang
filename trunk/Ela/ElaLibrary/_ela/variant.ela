/*
  Module variant
  Created in 2012 by Basil Voronkov
*/
import libCore#elalib@core

tagIt (x is variant) = core.createVariant (tag x)

fromSome (Some x) = x

fromLeft (Left x) = x

fromRight (Right x) = x

x `tagged` y = tag x == tag y