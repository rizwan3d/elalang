let @(|>)(x, y) = y(x);

let ~(&|)(x) = x + 100;

var pow(x, y) = x ** y;

let arr = [| 0, 1, 2 |];

let matchArray(arr) =
	| (x, y, z) -> "yep"
	| _ -> "nop";
		
//let (e1, e2, e3, e3) = arr;
	
cout matchArray(arr);

let private first_n(l, n) =
	| _, 0 			-> []
	| x::xs, _ 		-> ( x::first_n(xs, n - 1) )
	| [], _ 		-> (throw "too short");