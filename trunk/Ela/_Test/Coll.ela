let fold(seq, fun, initVal) =
{
	var ret = initVal;

	for (e in seq)
		ret = fun(e, ret);
		
	ret
}

let map(seq, fun) =
	| is list -> 
	{
		var ret = [];
		
		foreach (var e in seq)
			ret = fun(c)::ret;
	}
