open "$Guid"#elalib@g 
    (
        guid,
        parse
    )

let x == y = g.guidEqual x y
 on Guid#->Guid#

let x <> y = not (g.guidEqual x y)
 on Guid#->Guid#

let showf _ v = g.guidToString v
 on String#->Guid#