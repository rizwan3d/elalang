open Char[elalib];

let upper = Char.upper; //upper(x)

let lower = Char.lower; //lower(x)

let isLower = Char.isLower; //isLower(x)

let isUpper = Char.isUpper; //isUpper(x)

let toString(c) c :> string;